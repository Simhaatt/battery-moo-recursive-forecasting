# Repository conversion audit

## Verified

- Final optimizer logs contain exactly 12,600 rows: 3 methods × 15 runs × 280 evaluations.
- Search domains, learning-rate bounds, and `L+H≤29` hold for every recorded trial.
- Final mean hypervolumes reproduce as 1.197236, 1.157911, and 1.141019.
- Pooled nondominated set contains 28 records with contributions 21/2/5.
- Ten-seed NSGA-II macro MAPE, manual comparison, confidence interval, Wilcoxon result, and parameter reduction reproduce from deposited CSVs.
- Stress delta and transfer delta reproduce from seed/cell-level outputs.
- Fixed split cell and record counts agree with the manuscript.

## Reconstructed

- Publication tables in `results/generated/` are rebuilt from raw/summary artifacts.
- Core convergence, Pareto, parity, stress, and transfer plots are regenerated by clean package code.
- The public `Rpulse0` terminology is mapped from archived `Rct0` without changing values.
- Cell-level fixed split and grouped-fold files were extracted from the final processed inputs; raw record-level third-party data were not copied.

## Unresolved

- Full provenance for `k_exp`, SOC/type encodings, and activation-energy regularization.
- Exact cause of the conflicting superseded transfer artifact.
- Bitwise retraining reproducibility across GPU/software environments.

## Discrepancies

The superseded transfer file in `results/audit/superseded_transfer_cell_level_tests.csv` favors source transfer by about 3.2764 points. The final evidence pack in `results/transfer/transfer_cell_level_tests.csv` instead shows source adaptation is worse than target-only training by 6.987501 points. The final manuscript and expected-value contract use the latter. This discrepancy is intentionally visible and tested.

## Final status

Run `python analysis/reproduce_all.py`. At repository creation, all manuscript-contract and search-integrity checks return **PASS**. This status establishes internal numerical consistency of deposited artifacts; it does not claim an independent rerun from raw data.

